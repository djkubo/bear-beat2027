# Render Blueprint - Bear Beat
# Conecta tu repo en Render y Render usará este archivo para crear el servicio.
# Variables obligatorias en Dashboard: HETZNER_*, HETZNER_VIDEOS_BASE_PATH=Videos Enero 2026,
# NEXT_PUBLIC_APP_URL=https://tu-app.onrender.com, Supabase, Stripe, PayPal, etc.

services:
  - type: web
    name: bear-beat
    runtime: node
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
